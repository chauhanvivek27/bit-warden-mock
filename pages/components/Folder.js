import React from "react";
import filterStyles from "../../styles/folder.module.scss";

export default function AddFolder() {
  return (
    <div className={filterStyles["modal-header"]}>
      <h2 className={filterStyles["modal-header_title"]}> Add Folder</h2>
    </div>
  );
}
